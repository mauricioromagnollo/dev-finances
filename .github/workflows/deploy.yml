name: Deploy GH Pages

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: |
          mv ./public/* .
          rm README.md LICENSE *.js *.json
          rm -rf ./public
          rm -rf ./scripts
          rm -rf ./.github
          git add -A
          git commit -m "Deploy"
          git push && git checkout master

